<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.sprite.android.template">

    <application
        android:name="com.sprite.android.template.APP"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/appName"
        android:roundIcon="@mipmap/iconround"
        android:supportsRtl="true"
        >

        <activity android:name="com.sprite.android.template.MainActivity" android:theme="@style/Theme.AppCompat">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

    </application>

</manifest>